# Claude 简体中文管理器

双击仓库根目录的 `Claude简体中文管理器.cmd` 即可使用。

管理器会自动识别当前安装的 Claude Desktop 版本，并显示以下状态：

- **汉化已安装**：当前版本的中文资源、语言注册和语言设置均正常。
- **Claude 更新后需要修复**：Claude 已换到新版本目录，点击“修复汉化”即可重新适配。
- **汉化不完整**：部分文件缺失或语言设置不一致，点击“修复汉化”。
- **尚未汉化**：点击“一键汉化”。

“卸载汉化”只移除中文资源并恢复本工具备份的前端文件，不会卸载 Claude，也不会读取或修改账号、API 密钥、Gateway 或 3P 模型配置。

## 安全边界

- 使用安全模式，不修改 `Claude.exe` 和 `app.asar`。
- 每个 Claude 版本单独备份，拒绝把旧版本文件恢复到新版本。
- 只临时取得必要文件或目录的写权限，完成后恢复原访问控制。
- 如果 Claude 更新后语言注册结构发生变化，安装会失败并保留日志，而不会盲目批量替换 JavaScript。

日志位于仓库根目录的 `install-windows.log`，也可在管理器中点击“查看日志”。
